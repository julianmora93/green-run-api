import { initializeApp } from 'firebase-admin/app';

export class Security {

    constructor(){
        
    }

    validateToke(){
        //const auth = getAuth();
        //auth.verifyIdToken()
    }

}